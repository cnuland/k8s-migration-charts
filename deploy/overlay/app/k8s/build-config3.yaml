
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: migration-charts-build3
spec:
  output:
    to:
      kind: ImageStreamTag
      name: migration-charts:latest
  source:
    type: Git
    git: 
      uri: "https://github.com/cnuland/k8s-migration-charts"
      ref: "main"
    contextDir: "app/"
  strategy:
    dockerStrategy:
      from:
        kind: ImageStreamTag
        name: ruby:latest
    type: Docker
  postCommit: {}
  triggers:
  - type: ConfigChange
