
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: migration-charts-build
  namespace: migration-charts
spec:
  output:
    to:
      kind: ImageStreamTag
      name: migration-charts:latest
  source:
    type: Git
    git: 
      uri: "https://github.com/cnuland/k8s-migration-charts"
      ref: "main"
    contextDir: "app/"
  strategy:
    dockerStrategy:
      from:
        kind: ImageStreamTag
        name: ubi8-python:latest
    type: Docker
  postCommit: {}
  triggers:
  - type: ConfigChange
